/* Production API configuration.
   The frontend uses this Render backend for authentication,
   payment receipt submission, and permanent activation. */
window.ENV_API_URL = "https://pe-platform-algeria.onrender.com";
